# Build artifacts
bin/
obj/
publish/
tasks/
dist/
release-*/
*.dll
*.pdb
*.exe

# Plugin packages
*.zip
*.rar
*.7z
*.nupkg

# IDE files
.vs/
.vscode/
*.swp
*.lnk

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
*.pyo

# Temporary files
*.tmp
*.temp
*.log
benchmarks/
models/*.onnx

# Jellyfin specific
data/
plugins/
cache/

# Backup directories
backup_*/
.backups/
.archive/
JellyfinUpscalerPlugin-Backup*/
JellyfinUpscalerPlugin_Backup_*/
JellyfinUpscalerPlugin-v*/
apps/
apps-fork/

# Old publish directories
publish_*/

# Temp/debug files
harmony.log.txt
temp_js.js
issue39*
release_notes_*.md

# Old batch scripts
commit*.bat
final-commit.bat
git-commit.bat
build.bat

# Agent/AI tool artifacts
.zenflow/
docs/superpowers/

# Website/wiki (separate repos)
website/
wiki/
JellyfinUpscalerPlugin.wiki/
truenas-apps-submission/
truenas-catalog/

# Local Netlify folder
.netlify

# MCP/Agent tool caches
.claude-flow/

# Anime4K vendor build harness — keep the recipe (package.json/entry.mjs/build.mjs),
# ignore the install + generated artifacts.
.a4k-build/node_modules/
.a4k-build/out.js
.a4k-build/package-lock.json
.a4k-build/UPSTREAM_LICENSE.txt

# v1.8.3.13 - local build scratch that must never be committed
.a4k-build/pub*/
.a4k-build/ZIP_MD5.txt
.a4k-build/preview.html

# v1.8.3.14 - release staging dirs ("publish_*/" above misses the hyphen form) and
# local agent state. A git add -A once committed 6 MB of build artifacts from these.
publish-*/
zip-stage-*/

# Claude Code: keep local agent state out (the original intent of ".claude/"), but track
# the shared project config. A cloud session starts from a fresh clone, so settings.json
# and hooks/ only reach it if they are committed. Everything else under .claude/ stays out.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json
