# FastAPI and Server
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=1.2.2,<2.0

# AI/ML - ONNX Runtime CPU fallback
onnxruntime>=1.20.0,<2.0.0
numpy>=1.24.0,<2.0.0

# ncnn Vulkan — installed separately in Dockerfile with fallback chain
# (realsr-ncnn-vulkan-python -> ncnn -> build from source)

# Image Processing - OpenCV with DNN Super Resolution
opencv-contrib-python>=4.10.0,<5.0

# Image Processing - Pillow required by realsr-ncnn-vulkan-python
Pillow>=12.1.1,<13.0

# Utilities
httpx>=0.26.0
