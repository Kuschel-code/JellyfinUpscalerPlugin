pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
Pillow>=12.2.0,<13.0
numpy>=1.24.0
python-multipart>=0.0.30,<1.0
fastapi>=0.115.0
starlette>=1.0.1
# app/object_mask.py imports cv2 at module level, so without this the object-masking
# tests do not fail in CI - they fail to COLLECT, and pytest reports an error for a
# file nobody looks at while the suite looks broken for an unrelated reason. Stubbing
# cv2 like the client fixture does is not an option here: these tests assert on real
# pixels (a filled box, a blurred region, a letterbox pad), and a MagicMock would let
# them pass without testing anything. headless = no GUI libs on the runner.
opencv-python-headless>=4.8.0,<5.0
# test_detect_mask_endpoints.py builds real ONNX detector graphs and drives the
# endpoints through them. Without these it would importorskip - and a skipped test
# reads as a passing one, which is the same trap as the collection error above.
# CI-only: these never reach an image, so the "no plain onnxruntime beside
# onnxruntime-rocm/-openvino" rule in CLAUDE.md is not in play here.
onnx>=1.15.0
onnxruntime>=1.17.0
