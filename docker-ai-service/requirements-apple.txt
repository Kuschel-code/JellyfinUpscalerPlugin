# Apple Silicon (M1/M2/M3/M4/M5) requirements
# For native macOS installation or Docker on Apple Silicon

# FastAPI and Server
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=1.2.2,<2.0

# AI/ML - ONNX Runtime (CPU mode in Docker, CoreML natively)
onnxruntime>=1.20.0,<2.0.0
# coremltools — only works on native macOS, NOT in Docker (no linux/arm64 wheel).
# Install manually for native macOS usage: pip install coremltools>=7.0
numpy>=1.24.0,<2.0.0

# Image Processing - OpenCV with DNN Super Resolution
opencv-contrib-python>=4.10.0,<5.0

# Image Processing - Pillow required by realsr-ncnn-vulkan-python (ncnn path)
Pillow>=12.1.1,<13.0

# Utilities
httpx>=0.26.0
