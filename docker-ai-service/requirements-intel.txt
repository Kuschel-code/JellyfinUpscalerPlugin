# FastAPI and Server
fastapi>=0.115.0,<1.0
starlette>=1.0.1,<2.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.30,<1.0

# AI/ML - ONNX Runtime with OpenVINO EP
# Use onnxruntime-openvino which BUNDLES the OpenVINOExecutionProvider.
# Plain onnxruntime from PyPI does NOT include OpenVINO EP even with system OpenVINO
# present in the base image — verified empirically via issue #69 (Laurent's /gpu-verify
# returned onnx_providers: ["AzureExecutionProvider", "CPUExecutionProvider"] without
# OpenVINO despite running on openvino/ubuntu22_runtime:2025.4.1).
onnxruntime-openvino>=1.20.0,<2.0.0
numpy>=1.24.0,<2.0.0

# Image Processing - OpenCV with DNN Super Resolution
opencv-contrib-python>=4.12.0,<5.0

# Image Processing - Pillow required by realsr-ncnn-vulkan-python (ncnn path)
Pillow>=12.2.0,<13.0

# Utilities
httpx>=0.26.0
