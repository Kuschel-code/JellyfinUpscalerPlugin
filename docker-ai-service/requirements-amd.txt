# AMD ROCm specific requirements
# For AMD GPUs (RX 6000/7000 series, MI series)

# FastAPI and Server
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.7

# AI/ML - ONNX Runtime (ROCm version attempted first in Dockerfile, fallback to CPU)
onnxruntime>=1.20.0,<2.0.0
numpy>=1.24.0,<2.0.0

# Image Processing - OpenCV with DNN Super Resolution
opencv-contrib-python>=4.10.0,<5.0

# Utilities
httpx>=0.26.0
